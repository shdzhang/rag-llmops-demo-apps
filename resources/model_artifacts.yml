resources:
  registered_models:
    corporate_chatbot:
      name: ${var.model_name}
      catalog_name: ${var.catalog_name}
      schema_name: ${resources.schemas.corporate_schema.name}
      comment: "Corporate Affairs RAG Chatbot - Databricks App with async AgentServer"
      grants:
        - privileges:
            - EXECUTE
          principal: account users
